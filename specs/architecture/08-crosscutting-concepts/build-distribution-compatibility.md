# Build, Distribution, and Compatibility

Canonical skills generate portable and OpenCode assets. Generated outputs are
checked for parity and undeclared files. Direct Git distribution is self-contained;
the npm package is an OpenCode adapter with peer range `>=1.18.29 <1.19.0`.
Compatibility checks are pinned, hostless where possible, and no-network/no-secret.
