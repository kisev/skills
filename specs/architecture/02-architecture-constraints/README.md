# Architecture Constraints

The architecture preserves direct Git self-containment, an OpenCode peer range
of `>=1.18.29 <1.19.0`, explicit ownership, no-network hostless evals, and
content-addressed archival. Normative constraints are defined once in
`requirements/constraints/README.md`; this view records their architectural
consequences only.
