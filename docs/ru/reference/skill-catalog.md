# Каталог навыков

[English](../../reference/skill-catalog.md)

Текущий стабильный выпуск содержит 27 переносимых Agent Skills. Дистрибутив
определяется метаданными выпуска и контрольными суммами архивов; у отдельных
навыков нет версии. Их процессы автономны, и навыки можно устанавливать
независимо от `@kisev/skills-opencode`.

## Активные навыки

| Навык | Назначение |
| - | - |
| `agents-md` | Создать или проверить инструкции `AGENTS.md`, действующие в пределах репозитория. |
| `askme` | Уточнить неполную задачу или проектное решение с помощью ограниченного набора вопросов. |
| `ast-grep` | Выполнить структурный поиск или безопасное прямое преобразование AST с помощью ast-grep. |
| `code-explain` | Составить обзорную карту текущих незавершённых изменений, диапазона Git, ветки или истории MR без внесения изменений. |
| `code-review` | Проверить GitLab MR или локальные незавершённые изменения на дефекты и риски. |
| `commit-msg` | Подготовить одно краткое сообщение коммита на английском языке по локальным изменениям. |
| `docs-prepare` | Подготовить пользовательский документ на основе проверенных данных прямо в проекте. |
| `docs-review` | Проверить пользовательскую документацию на точность и удобство. |
| `goal` | Подготовить структурированную цель в Markdown объёмом не более 4000 символов без изменения файлов. |
| `humanize` | Сделать технический текст прямым и естественным. |
| `mattermost` | В заданных границах прочитать и разобрать публикацию, обсуждение, канал или чат Mattermost. |
| `mr-prepare` | Подготовить метаданные и локальный план публикации для GitLab MR. |
| `release-prepare` | Подготовить MR выпуска, инвентарь, объявление и план публикации. |
| `release-review` | Проверить MR выпуска на полноту и совместимость. |
| `rtk` | Выборочно использовать RTK для сжатия многословного вывода команд. |
| `skill-improve` | Проверить и улучшить один Agent Skill за несколько итераций. |
| `slides-prompts-prepare` | Соединить выбранную тему презентации с фактическими сведениями о команде и технологиях. |
| `spec-manage` | Создать спецификации нового проекта, описать существующий, изменить каноническое целевое состояние или проверить спецификации без записи; token режима необязателен. |
| `stopit` | Записать для следующего сеанса передачу контекста с удалёнными чувствительными данными. |
| `briefing` | Превратить расшифровки, заметки или исследование в структурированную фактическую сводку. |
| `task-prepare` | Подготовить задачу или согласованный набор задач GitLab с планом ручной публикации. |
| `task-review` | Проверить рабочую задачу, не привязанную к системе хранения, без изменения внешнего состояния. |
| `task-triage` | Разобрать явно предоставленные материалы рабочей задачи без записи и привязки к системе хранения. |
| `team-retro` | Подготовить ретроспективу или презентацию о результатах на основе данных непубличного профиля. |
| `team-roadmap` | Проверить или обновить план развития на основе данных непубличного профиля. |
| `team-sprint-close` | Завершить один цикл спринта по непубличному профилю или явно указанному контексту. |
| `team-sprint-start` | Начать один цикл спринта по непубличному профилю или явно указанному контексту. |

Точные списки активных и устаревших имён приведены в
[инвентаре миграции](../migration-inventory.md).

## Режимы спецификации

`spec-manage` принимает явные tokens `spec-init`, `spec-onboard`, `spec-update` и
`spec-audit`, но обычный запрос может выражать намерение естественным языком.
Перед выбором между новым и существующим проектом навык проверяет данные
репозитория. Если после этого подходят несколько режимов, он задаёт один короткий
вопрос и ничего не записывает до ответа.

- «Создай каноническую спецификацию для этого нового пустого проекта» выбирает `spec-init`, только если нет значимого кода, тестов, схем, конфигурации, CI или deployment.
- «Опиши этот существующий сервис в канонических спецификациях» выбирает `spec-onboard`, если в репозитории уже есть реализация, но нет `specs/`.
- «Измени каноническое целевое время ожидания на 30 секунд» выбирает `spec-update`, если `specs/` существует.
- «Проверь эти спецификации по реализации, ничего не меняя» выбирает read-only режим `spec-audit`.

Запросы на реализацию, план, roadmap или пользовательскую документацию не
выбирают `spec-manage` только из-за упоминания требований или архитектуры.

## Командные профили

Командные навыки по умолчанию ищут непубличный профиль в
`${XDG_CONFIG_HOME:-~/.config}/opencode/team-contexts/`. При первом запуске они
могут собрать его из ответов пользователя и явно указанных файлов, URL,
репозиториев или данных коннекторов, запросить только недостающие поля и сохранить
после предварительного просмотра с подтверждением. Просьба запомнить участника,
проект, источник или предпочтение по оформлению обновляет непубличный профиль, а
не общедоступный навык.

Версионированная общедоступная схема и обезличенный пример входят в каждый
командный навык как `references/team-context.schema.json` и
`references/team-context.example.json`. Профили остаются вне этого репозитория;
не храните в них учётные данные или личные заметки.

## Требования и ограничения

- Если переносимому навыку нужен исполняемый скрипт, он использует только
  стандартную библиотеку Python 3.12+.
- `ast-grep` и `rtk` требуют внешних CLI; сами навыки их не устанавливают.
- Переносимые навыки работают без `@kisev/skills-opencode`.
- Навыки не заменяют правила репозитория, проверку кода, поиск секретов,
  управление доступом и итоговое решение пользователя.
